# ZlibCore Node.js Workspace

Demonstrates server-side compression.

## Usage

1. Install dependencies:
   ```bash
   npm install
   ```
   *Note: Uses local zlibcore reference.*

2. Run:
   ```bash
   node index.js
   ```

3. Check `output.zlib`.
