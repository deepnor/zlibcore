# ZlibCore Browser Workspace

Demonstrates client-side compression using Native ES Modules.

## Usage

1. Serve the root directory (to resolve `../../src/`):
   ```bash
   npx serve ../../
   ```
2. Open `examples/workspace-browser-client/index.html` in browser.

## Logic

Uses `ZlibCore.compress(Uint8Array)` directly.
